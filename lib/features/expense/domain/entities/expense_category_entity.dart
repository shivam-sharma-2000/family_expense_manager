import 'package:flutter/material.dart';

class ExpenseCategory {
  final String name;
  final dynamic icon;
  final Color color;

  const ExpenseCategory({
    required this.name,
    required this.icon,
    required this.color,
  });
}
